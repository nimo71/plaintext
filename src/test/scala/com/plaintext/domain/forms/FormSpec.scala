package com.plaintext.domain.forms

import org.scalatest._
import org.scalatest.matchers.ShouldMatchers._

class FormSpec extends FlatSpec {

    "Form foldLeft" should "Fold over all form fields" in {
        fail()
    }
}